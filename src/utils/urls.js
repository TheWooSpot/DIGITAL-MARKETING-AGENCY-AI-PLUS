/**
 * Report URL helpers (TypeScript implementation).
 * Re-export for docs/tooling that reference `urls.js`; app code may import `@/utils/urls` (TS).
 */
export { generateReportLink } from "./urls.ts";
